flysystem:
    storages:
        default.storage:
            adapter: 'memory'

        file_source.storage:
            adapter: 'memory'

        run_source.storage:
            adapter: 'memory'

        git_repository.storage:
            adapter: 'memory'

        test_fixtures.storage:
            adapter: 'local'
            options:
                directory: '%fixtures_base_path%'
